---
import Card from './Card.astro';
import Section from './Section.astro';
import speakers from '../data/speakers.json';
---

<Section id="speakers" title="Oradores">
  {speakers.length > 0
    ? speakers.map((speaker) => (
        <Card
          name={speaker.name}
          role={speaker.role}
          picture={speaker.picture}
          socials={speaker.socials}
          variant="blue"
        />
      ))
    : <p class={`text-lg font-semibold text-white`}>No hay oradores disponibles en este momento.</p>
  }
</Section>
